#!/usr/bin/python3

def number_keys(a_dictionary):
    """
    Finds the number of keys in a dictionary
    Args:
        a_dictionary: dictionary
    Return: integer
    """
    return len(a_dictionary.keys())
